# Map Terrain Raster — Progress Ledger

Base commit (branch start): a049252
Plan: docs/superpowers/plans/2026-07-07-map-terrain-raster.md
Workspace: worktree `.claude/worktrees/map-terrain-raster`, branch `map-terrain-raster` (manually created from local HEAD, not via EnterWorktree's default name-mode, since local main was ahead of origin/main — see memory unity-subagent-driven-dev-lessons)

Task 1: complete (commits 26934d1..5c86556, review clean — Approved)
Task 2: complete (commits 5c86556..bf94641, review clean — Approved; plan's code block was missing `using WorldGen.Generation;`, implementer fixed)
Task 3: complete (commits bf94641..a62f4e7, review clean — Approved; all 112 color-table values machine-diffed against brief, zero discrepancies)
Task 4: complete (commits a62f4e7..935fe38, review clean — Approved, verified bit-for-bit identical delegation)
Task 5: complete (commits 935fe38..0a41c12, review clean — Approved; self-test pixel/cell math hand-verified by reviewer)
Task 6: complete (commits 0a41c12..39ed93b, review clean — Approved on Opus; every load-bearing formula/ordering line-verified against brief, self-test fallback path hand-traced correct)
Task 7: complete (commits 39ed93b..3642432, review clean — Approved on Opus; plan's brief missed 3 of 12 RecolorOnly call sites, implementer found+fixed all, reviewer verified completeness; UV/pixel/cellId mapping cross-checked consistent with MapRasterizer)
Task 8: complete (commits 3642432..1de93a9, review clean — Approved)
Task 9: complete (commits 1de93a9..3479c9f, review clean — Approved)
Task 10: complete (automated checks done; user's live Play Mode pass found 3 real bugs, all fixed — see below; user confirmed on re-test "Да, баги пофикшены, насколько могу судить" — bugs fixed as far as they can tell)

## Live-testing bug fixes (found during Task 10's Play Mode pass, outside the plan's own tasks)
- Crash: degenerate cells (Polygon.Count < 3, never classified during generation, Biome defaults to C# `Ocean`=0 while IsOcean=false) reachable via NearestCellLookup, tripped MapPalette's fail-loud Sea/Lake guard. Fixed: NearestCellLookup excludes Polygon.Count < 3 cells (matches old fan-mesh renderer's own convention).
- Horizontal stripes every ~64 rows: RebakeAllStepped baked cellId+fields+color+vignette per chunk, so each chunk's last row read the next (not-yet-baked) chunk's zero-valued elevation for its hillshade gradient. Fixed: MapRasterizer.RebakeRegion split into BakeFieldsRect (order-independent) + ColorAndVignetteRect (needs neighbor rows already baked); RebakeAllStepped now bakes fields for the whole image once before chunking the color pass.
- Layer toggles (Биом/Рельеф in MapLayersPanel) had zero visual effect in Combined+smoothBorders mode (never wired into MapRasterConfig) while still triggering a full slow RebakeAll(). Fixed: added ShowBiomeLayer/ShowReliefLayer to MapRasterConfig, wired from BuildRasterConfig, honored in ColorForLandPixel/ColorForWaterPixel.
- Commits: 96adfc3 (all three fixes + 3 new self-tests), cd211de (follow-up: independent review found the Polygon<3 guard in 96adfc3 broke 6 self-tests whose fixture cells never set Polygon — fixed with a shared SquarePolygon(site) test helper). Both commits reviewed clean (96adfc3's review found the fixture-polygon issue as Critical; cd211de's re-review confirmed it fully fixed, no other issues, Approved).
- User separately raised a bigger, not-yet-actioned design question: current Combined+smoothBorders look reads as "mostly water/blob" and worse than the old flat-polygon view; wants stronger land/water contrast, optionally flatter per-region fill instead of smooth blending, vector-style coastlines like the mockup, and asked whether the map could be a genuinely vector image (with a separate polygon view for editing/brush, rendered view for display). Not yet resolved — flagged to revisit after bug-fix confirmation, likely needs superpowers:brainstorming given the scope (could affect subprojects 2-6 and the editing workflow).

## Final whole-branch review (opus, range a049252..cd211de)

Verdict: Ready to merge — with fixes. Cross-task composition sound (two-phase bake split, degenerate-cell exclusion, layer-toggle wiring, all 12 RecolorOnly→RebakeAll call sites migrated) all verified consistent; self-tests genuinely exercise the bugs they guard (SelfTestChunkedBakeContinuity confirmed to catch the naive-bake bug, not just pass tautologically).

- **Important (fixed, commit 6378ab8, re-reviewed Approved with no findings):** `rasterTexture` was never `Destroy`d before reassignment in `RebakeAll`/`RebakeAllStepped`, and there was no `OnDestroy` — every mode switch/override/undo/regeneration leaked up to ~16MB of native texture memory (old fan-mesh `RecolorOnly` allocated no texture at all, so this cost is new to this branch). Fixed: `Destroy(oldTexture)` before both reassignments + new `OnDestroy()` destroying `rasterTexture`/`rasterMaterial`. Re-review confirmed completeness (grepped all `rasterTexture=`/`rasterMaterial=` sites, all covered), no double-destroy/use-after-destroy risk, no collateral changes.
## Branch status

Sub-project 1/6 (base raster fill) is functionally complete and reviewed clean: all 10 plan tasks + 3 live-testing bug fixes + final whole-branch review's one Important finding (texture leak), all independently reviewed Approved. User chose "Оставить как есть" (keep as-is) at the finishing-a-development-branch checkpoint — branch `map-terrain-raster` and worktree `.claude/worktrees/map-terrain-raster` intentionally left un-merged/un-pushed, pending the still-open design-direction conversation (contrast/flat-fill/vector-rendering) which may itself produce further changes on this branch before it's finalized.

- **Minor (deferred, not blocking):** `BakeFieldsRect`'s `nearest.Id` deref is unguarded — would NRE only on a pathological all-degenerate-cells map (unreachable at real map scale). `ApplyDarknessRect`'s per-call `Color32[]` alloc is now on the brush hot path but bounded/transient (confirm-and-defer; `texture.Apply(false)` dominates anyway). Cosmetic: dead `ToWorldPos` method, stale `CellOverrideService.cs:17` doc comment referencing removed `RecolorOnly()`, self-tests use `Destroy` instead of `DestroyImmediate` on textures (no-ops harmlessly in Edit mode, logs a console warning).

## Follow-up feature: Coastline contour smoothing (2nd plan on same branch)

Spec: docs/superpowers/specs/2026-07-07-coastline-contour-smoothing-design.md (committed e3c0114)
Plan: docs/superpowers/plans/2026-07-07-coastline-contour-smoothing.md (committed 3dfa9b0) — 4 tasks
Base for this feature's tasks: 3dfa9b0
Pre-flight review done: caught that plan's original Task 3 left the build non-compiling (signature change, prod call sites deferred to Task 4); restructured so Task 3 updates all 8 call sites + prod calls pass existing `corners` field, Task 4 only adds the serialized `coastlineSmoothness` field + config wiring + brush self-test. Both tasks now independently compile/testable.

- Task 1: complete (commit 3dfa9b0..783cacd, review clean — Approved; reviewer stood in as compile-check, hand-traced fixture geometry + Chaikin counts, confirmed non-vacuous asserts. 2 Minor: (a) no test for the degenerate/open-chain skip branch — plan's Step 2 specified exactly one happy-path fixture, defer; (b) AddBoundaryNeighbor's Contains-dedup is unreachable given caller's neighborId<=corner.Id guard — harmless dead defensive code.)
- Task 2: complete (commit 783cacd..4f336c8, review clean — Approved; reviewer hand-traced the even-odd fill on the actual self-test fixture end-to-end + verified partial-rect write scoping + brace balance. 1 Minor: self-test never exercises exact-boundary tie-break cases (all vertices at integer coords, all samples at integer+0.5) — guard logic verified correct by hand, coverage-only gap, defer.)
- Task 3: complete (commit 4f336c8..00a8f73, review clean — Approved on opus; reviewer verified all 12 Bake/RebakeRegion/BakeFieldsRect call sites carry `corners` in the right position + ColorAndVignetteRect correctly untouched + WorldMapRenderer is the only caller file, traced the IsLand-mask categorization swap complete on every painted path, confirmed parity self-test is a real oracle. 1 Minor: `cell`/`cells` params now unused in ColorForLandPixel/BakeFieldsRect/BakePaintedFields — verbatim from brief, predate task, defer to cleanup.)
- Task 4: complete (commit 00a8f73..fd105b1, review clean — Approved; reviewer confirmed field declared/wired exactly once, single-file scope, and hand-traced the brush self-test fixture (pixel (22,10)→world (2.25,1.05) inside the edited cell, inside the second dirty rect, water→land flip real) — non-vacuous. 1 Minor: brush self-test hardcodes CoastlineSmoothness=2 so it validates the dirty-rect mechanism generically, not the new field→config wiring specifically (wiring is a trivial 1-line assignment, visually verified).)
- All 4 tasks complete + reviewed clean. Feature commits: 783cacd, 4f336c8, 00a8f73, fd105b1 (base 3dfa9b0).

### Final whole-feature review (opus, range 3dfa9b0..fd105b1) — Ready to merge: YES, no Critical/Important
Verified end-to-end composition across full/chunked-stepped/brush-partial bakes: signature threading complete (all 14 call sites pass `corners` correctly), categorization DECISION swap complete (every painted-mode land/water decision reads `!IsLand[idx]`; residual EffectiveIsOcean/IsLake reads are legit color-source selection only), rect-scoped RasterizeIsLand provably correct for partial rebakes (scans crossings from row's left edge so parity at rectX is established from crossings outside the rect), chunked-bake seam preserved (BakeFieldsRect runs once whole-image in RebakeAllStepped before any ColorAndVignetteRect chunk), `corners` guaranteed non-null in painted bake, trace is O(corners) called once per bake, degenerate chains skip without throw. 4 Minor findings (reviewer said ship as-is, all within spec's accepted scope):
  1. All-land map renders as all-water: if brush ForceLand removes the LAST water cell, TraceSmoothedLoops returns empty → RasterizeIsLand writes all-false (=all water). Unreachable at generation (edge falloff guarantees ocean border) but reachable via brush. Contrived hard cliff; one-line guard ("no loops AND ≥1 land cell → all land") removes it.
  2. Land pixel whose nearest cell is water with no land cell within SmoothRadius → sumW<=0 fallback takes FamilyColor from the water cell's Biome (ocean slot). Rare (Chaikin biases contour inward), cosmetic fringe at smoothed concave bays past SmoothRadius. Visual spot-check.
  3. Dead param: `ColorForLandPixel` (MapRasterizer.cs:360) no longer reads its `cell` arg; `cells` threaded through BakeFieldsRect→BakePaintedFields unused (latter predates feature). No warning/behavior impact; safe tidy-up (keep `cell` in BakePaintedPixel/ColorForWaterPixel — water path uses it).
  4. RasterizeIsLand scans all loop edges per scanline even for a tiny brush rect: O(rectH × E_total), E_total ~×2^smoothness. Design doc explicitly accepted full-retrace cost (YAGNI). Monitor; if brush lag appears, Y-bucket loop edges or clip to rect X-span, not incremental.
Not compiler-verified (Editor locked) — reviewer hand-traced types/usings/signatures, found nothing that won't compile.

### Post-review cleanup (commit 599fbb8) — findings #1 + #3 applied at user's request
- #1: BakeFieldsRect now guards `loops.Count == 0` — if any cell is land, fills the rect IsLand=true (all-land) instead of letting even-odd write all-water; else all-water. Only triggers when no coastline exists (brush ForceLand over last water cell); normal maps always have a coastline so the else branch (RasterizeIsLand) is unchanged. No existing self-test changes behavior (parity + brush tests both keep a coastline → else branch).
- #3: dropped unused `VoronoiCell cell` param from ColorForLandPixel + its one call site in BakePaintedPixel (kept on ColorForWaterPixel — water path uses it for lake-vs-ocean color).
- Findings #2 (concave-bay color fringe) and #4 (RasterizeIsLand per-scanline edge scan perf) left as-is per review (cosmetic / YAGNI-deferred). Applied changes are manual-review-only until Editor recompiles.

## Follow-up feature: Wide coastline glow (3rd plan on same branch)

Spec: docs/superpowers/specs/2026-07-07-coastline-glow-width-design.md (committed 6b242db)
Plan: docs/superpowers/plans/2026-07-07-coastline-glow-width.md (committed 21a42e1) — 2 tasks
Base for this feature's tasks: 21a42e1
Goal: widen only the water-side light glow (keep the 1px dark land outline) into a broad soft halo via a chamfer coast-distance field (cost independent of glow width). New field coastlineGlowWidth (int 0-64, default 16).
Pre-flight review: clean — Task 1 adds a utility method + CoastDistance buffer (no signature changes, compiles standalone), Task 2 consumes it. No inter-task compile break.

- Task 1: complete (commit 21a42e1..8d13161, review clean — Approved, zero findings; reviewer hand-traced the chamfer math against both fixtures (not just transcription), confirmed neighbor offsets/weights exact, and verified the seam test is genuinely discriminating (sub-rect has no land inside → correctness depends entirely on out-of-rect buffer reads).)
- Task 2: complete (commit 8d13161..fd8008c, review clean — Approved; reviewer verified all 6 edits formula-exact + dark outline untouched (HasNeighborWithWaterStatus still single-called with wantWater:true, old water-side wantWater:false call fully removed, no dead code) + field-name consistency across config/BuildRasterConfig/DT-call/pad + both self-tests non-vacuous (delta-vs-glowWidth=0 technique cancels ripple noise). 1 Minor: ComputeTouchedPixelRect xml-doc still only mentions smoothRadius, not the new glowWidth term — fixed post-review, doc-only.)
- Both tasks complete + reviewed clean. Feature commits: 8d13161, fd8008c, f47fc42 (doc nit) (base 21a42e1).

### Final whole-feature review (opus, range 21a42e1..f47fc42) — Ready to merge: YES, no Critical/Important
Traced all 3 bake paths (full/chunked-stepped/brush-partial). Confirmed: DT is O(pixels), glow-width-independent (single CoastDistance[idx] read, no rescan); chunked seam avoided (BakeFieldsRect runs once whole-image → CoastDistance fully populated before any ColorAndVignetteRect chunk; glow read has ZERO neighbor dependency so seam-free by construction); DT ordered after final IsLand (both the all-land guard branch and RasterizeIsLand complete before the DT call); glowWidth=0 double-guarded (DT skipped AND glowT=0 → CoastDistance never read); dark outline untouched (HasNeighborWithWaterStatus single caller wantWater:true, old water call gone, no dead code); compile trace clean. 3 Minor:
  1. Pad uses X world-per-pixel factor for both axes — verified harmless (aspect preserved mod RoundToInt, <0.1%, absorbed by Floor/Ceil slack + smoothRadius). FIXED: clarified the inline comment (no code change).
  2. No end-to-end brush-glow color-seam test — logic already covered by (DT seam-safe) + (color reads only CoastDistance[idx], no neighbor); low risk. Deferred (add when Editor free).
  3. Future subproject-6 glowWidth slider MUST route through RebakeAll not RebakeRegion (partial DT seeds from buffer clamped at previous glowWidth+1 → raising width + partial-only rebake would under-glow at rect edge). Unreachable now (no UI/OnValidate). RECORDED durably in the glow design spec's Риски section.
Not compiler/test-verified (Editor locked) — reviewer hand-traced signatures/types/division, found nothing that won't compile.

## Follow-up feature: Flat region fill + elevation bands (4th plan on same branch)

Spec: docs/superpowers/specs/2026-07-07-flat-region-fill-design.md (committed 3cbf6b6)
Plan: docs/superpowers/plans/2026-07-07-flat-region-fill.md (committed eb0e82b) — 2 tasks
Base for this feature's tasks: eb0e82b
Goal: toggle flatRegionFill (user-default on) — replace smooth cross-biome blend with flat per-region fill: color = nearest-cell biome family modulated by discrete elevation bands (higher=lighter), even tone + light grain. Skips the expensive BakePaintedFields blend pass. Water/smoothed-coast/glow unchanged. Approach A: faceted internal biome/band boundaries (smoothing them is a possible future iteration). Params: flatRegionFill(bool), elevationBands(int 2-8, def 5), elevationBandContrast(float % 0-100, def 40).
Pre-flight review: clean. NOTE the deliberate default asymmetry — MapRasterConfig.FlatRegionFill defaults FALSE (so existing painted self-tests that don't set it keep the blend path and don't break on buffers.Elevation being unpopulated), while the serialized WorldMapRenderer.flatRegionFill defaults TRUE (user-facing "on"). Between Task 1 and Task 2 production is temporarily on blend (BuildRasterConfig not yet wired).

- Task 1: complete (commit eb0e82b..48faf1c, review clean — Approved; reviewer traced band formula (elev 0.1/0.5/0.9→factor 0.8/1.0/1.2), verified config default false + blend path untouched + guard leaves IsLand/CoastDistance unconditional + all 3 self-tests non-vacuous (checked against actual palette table + grain amplitude + confirmed all-land guard makes IsLand uniformly true so no coast contamination). 1 Minor: no direct ElevationBands=1 divide-by-zero regression test (code verified correct via the ElevationBands>1 gate; coverage-only).)
- Task 2: complete (commit 48faf1c..bebdcef, review clean — Approved, zero findings; reviewer verified 3 fields correct type/attr/default/position + wiring names match MapRasterConfig case-sensitively (independently checked MapRasterizer.cs) + diff scoped to WorldMapRenderer.cs only, 9 insertions/0 deletions).
- Both tasks complete + reviewed clean. Feature commits: 48faf1c, bebdcef (base eb0e82b).

### Final whole-feature review (opus, range eb0e82b..0c1f3d3) — Ready to merge: YES, no Critical/Important
Traced all 3 bake paths in flat mode. Confirmed: default asymmetry composes (every existing painted self-test builds config WITHOUT FlatRegionFill → config-default false → blend path unbroken, incl. SelfTestRasterElevationInvariant which reads buffers.Elevation); ColorForLandPixelFlat reads ONLY cell.Biome/EffectiveElevation + IsLand + Noise (never the unpopulated Elevation/Temperature/FamilyColor); flat-mode water still works (cell+CoastDistance); chunked-stepped seam-safe by construction (flat colorer's only ±1 read is whole-image IsLand, no per-chunk buffer dep — unlike the old hillshade seam); perf claim holds (BakePaintedFields is the sole FindWithinRadius pass, only thing guarded off); API usage compiles (GetSlotColor(theme,family) overload, ElevationBands>1 gate prevents /0). 4 Minor:
  1. Stale doc on ColorAndVignetteRect (precondition now mode-dependent). FIXED (doc-only).
  2. Stale summary on BakeFieldsRect (BakePaintedFields now also needs !FlatRegionFill). FIXED (doc-only).
  3. varAmt computed per-pixel but unused in flat production (blend branch still uses it → not dead code). Left as-is per reviewer ("no change needed"); negligible (one division).
  4. No flat-mode chunked-continuity self-test (correct-by-construction, verified; blend-path SelfTestChunkedBakeContinuity covers the seam mechanism). Deferred (add flat variant when Editor free).
Recommendation (for user's Editor test): per memory unity-csharp-namespace-and-serialization-gotchas (Unity scene deserialization silently overrides C# field initializers, hit 3×), CONFIRM in the live Inspector that the 3 NEW serialized fields show true/5/40, not false/0/0. Brand-new fields have no stored scene override so standard Unity uses the initializers (code correct), but this is the documented project hazard worth eyeballing. If elevationBands deserialized 0 → ElevationBands>1 gate silently disables bands; if flatRegionFill false → feature off.
Spec-internal drift (not a code defect): spec §3 illustrative ColorForLandPixelFlat signature still shows idx/coldAmt params the plan+code correctly dropped. Plan is the accurate source.
Not compiler/test-verified (Editor locked) — reviewer hand-traced composition + API usage.

### Live crash fix (found during user's flat-fill Play Mode test)
Crash: ArgumentOutOfRangeException "Sea/Lake не имеют плоского слота" in MapPalette.FamilyToSlot(Sea) ← ColorForLandPixelFlat. Root cause (systematic-debugging): the smoothed coastline (Chaikin) reclassifies coastal fringe pixels as land (IsLand=true) whose NEAREST Voronoi cell is a water cell; flat fill reads GetFamily(cell.Biome) directly (no land-neighbor filter like the blend path has) → water family Sea/Lake has no flat palette slot → throw on every such fringe pixel. This is ALSO the same root cause as the coastline final-review finding #2 (which mislabeled it "cosmetic, not a crash" — it IS a crash, just rarer in the blend path's sumW<=0 fallback). Fix: new helper `MapRasterizer.FlatFamilyColor(theme, biome)` substitutes Coast for Sea/Lake families; used by both ColorForLandPixelFlat (flat path) and BakePaintedFields sumW<=0 fallback (blend path — defense-in-depth). Fringe becomes a coast tone (mostly hidden under the 1px dark outline anyway). New regression self-test `SelfTestFlatFillCoastalFringeNoCrash` (center-lake 3x3 fixture reliably drives a land-classified pixel over a water cell; asserts RebakeRegion doesn't throw). This resolves coastline-feature final-review Minor #2. Commit 6b7b241, re-reviewed (sonnet) Approved zero findings — reviewer confirmed FamilyToSlot throws only on Sea/Lake (guard covers exactly those), both call sites converted + the water-neighbor-filtered accumulation loop correctly left alone, and hand-traced the even-odd fill on the fixture to prove the test is non-vacuous (throws pre-fix, passes post-fix).

## Minor findings (for final-review triage)
- Task 1 (`Noise.cs:19`): `(uint)h / 4294967296f` narrows to float 24-bit mantissa before dividing, ~6e-8 relative deviation from JS's double-precision result — brief-specified code, not implementer-introduced, harmless for visual terrain gen.
- Task 2 (`NearestCellLookup.cs`): `MaxRingSearch = 128` cap contradicts the "null only if index is empty" doc comment (unreachable in practice at this project's map scale); `FindWithinRadius`'s ring span is one ring wider than strictly necessary (perf-only, errs safe); ring-0 early exit is a no-op except at exact distance 0 (perf-only). All three inherited verbatim from the plan's own code, not implementer choices.
- Task 5 (`MapRasterizer.cs`): `ApplyDarknessRect` allocates a new `Color32[]` on every call, not persisted on `MapRasterBuffers` — could become a GC hot-path once Task 7/8 wire brush-driven partial rebakes through it frequently; worth a look in final review if brush painting feels laggy. Also `Elevation`/`Temperature`/`FamilyColor` buffers are allocated but unused until Task 6 populates them (expected, not dead code once Task 6 lands).
- Task 7 (`WorldMapRenderer.cs`): dead private `ToWorldPos` method left in place (only the removed fan-mesh code called it) — candidate for later cleanup. `CellOverrideService.cs:17`'s doc comment still says "WorldMapRenderer.RecolorOnly()" (method no longer exists) — cosmetic, in a file this task correctly didn't touch. Brush painting won't visually update yet — expected, Task 8 wires `RebakeAffectedCells` into `BrushToolController` next.

## Follow-up feature: Curved biome + elevation-band borders + contour roundness (5th plan on same branch)

Spec: docs/superpowers/specs/2026-07-07-curved-biome-borders-design.md (committed 37cc64a)
Plan: docs/superpowers/plans/2026-07-07-curved-biome-borders.md (committed f5f1cad) — 4 tasks
Base for this feature's tasks: f5f1cad
Goal: in flat-fill mode, smooth (curve) two land categorical fields — biome family and elevation band — via approach A (per-category smoothed region masks reusing the coastline machinery), plus a shared vertex-decimation "roundness" control applied to coastline, family, and band contours. New config: SmoothRegionBorders(bool, cfg-default FALSE), BorderRoundnessDistance(float, cfg-default 0); serialized smoothRegionBorders(true)/borderRoundness(1). New buffers FamilyLabel/BandLabel (int[], -1 sentinel). Same deliberate default asymmetry as flatRegionFill (cfg-default off so existing self-tests keep old path; serialized on for user).
Pre-flight review (done at plan-authoring): fixed 3 compile hazards in the plan before execution — (1) CoastlineContour.cs lacked `using System;` for Func (added to Task 1 Step 1); (2) Task 1 originally upgraded the coastline call to 4-arg using BorderRoundnessDistance before that field exists — moved the 4-arg upgrade wholly into Task 2 Step 4, Task 1 leaves the 3-arg call (binds to water wrapper, decimation 0); (3) confirmed each task compiles independently. Also verified test math against VoronoiCell semantics (EffectiveElevation==Height, EffectiveIsOcean from IsOcean).

- Task 1: complete (commits f5f1cad..12f002d, review clean — Spec ✅ / Quality Approved on sonnet; reviewer independently re-traced overload resolution across all 6 TraceSmoothedLoops call sites (no ambiguity — general overload's required `inRegion` never supplied by 3-arg/named callers), re-derived the 12→6 decimation + 4-vertex guard + the label test's scanline crossings, recomputed brace balance 31/31 & 302/302, confirmed MapRasterizer.cs untouched. 1 Minor (spec-mandated, deferred): RasterizeRegionLabel duplicates RasterizeIsLand's even-odd scanline verbatim except the write condition — a shared crossings helper is a reasonable future dedup, out of scope here.)
- Chore (commit 12f002d..b2e1dbb): tracked `CoastlineContour.cs.meta` — implementer found it was untracked (the coastline feature committed the .cs without its .meta; Unity would regenerate a fresh GUID). One-line fix, unrelated to feature logic but same file family.
- Task 2: complete (commits b2e1dbb..458e516, review clean — Spec ✅ / Quality Approved on sonnet, ZERO findings; reviewer independently cross-checked all 11 CreateEmptyBuffers fields preserved + both labels -1-filled, water→-1 in both category fns (critical: keeps Sea/Lake out of labels so GetSlotColor never throws), FamilyPriority order exact, RasterizeSmoothedCategoryRect arg positions (w=TexWidth pos4, TexHeight pos5) correct against the 11-param RasterizeRegionLabel sig, BakeFieldsRect gating (family: FlatRegionFill&&SmoothRegionBorders; band: +ElevationBands>1; else BakePaintedFields) + 4-arg coastline upgrade, ColorForLandPixelFlat untouched, and hand-verified the 5x5 ocean-bordered fixtures close their loops + pixel/label/band math. Brace 62/62 & 322/322.)
- Task 3: complete (commits 458e516..b870ff0, review clean — Spec ✅ / Quality Approved on sonnet; reviewer confirmed ColorForLandPixelFlat rewrite exact (idx local; family/band from label when SmoothRegionBorders else -1 → nearest-cell fallback; both gates + outline + grain unchanged, correct order), (BiomeFamily)fLabel cast safe (land families only), diff scoped to that one method, and cross-checked the parity test is non-vacuous by reusing Task 2's proven label values at px (30,20)/(10,20) — on/off byte-identical in interior, wrong slot/band/throw would fail. Verified prod still unchanged (no serialized smoothRegionBorders yet, BuildRasterConfig doesn't set it). 1 Minor (verbatim-from-brief, deferred): the test's FAIL Debug.Log re-invokes Bake() twice to format the delta — costs 2 extra small bakes only on the rare failure path of a manual ContextMenu test.)
- Task 4: complete (commits b870ff0..24bb875, review clean — Spec ✅ / Quality Approved on sonnet, ZERO findings; reviewer confirmed 2 serialized fields (smoothRegionBorders=true, borderRoundness=[Range 0-3]=1f) in the right header block between elevationBandContrast and rasterLongSide, both wired in BuildRasterConfig with names matching MapRasterConfig case-sensitively (BorderRoundnessDistance = borderRoundness*minPointDistance resolves slider→world units), pad line gains + borderRoundness*minPointDistance with doc updated, single-file scope 13/3. Editor-side Inspector defaults eyeball (Unity serialization gotcha) correctly deferred to user.)
- All 4 tasks + meta chore complete + reviewed clean. Feature commits: 12f002d, b2e1dbb(chore), 458e516, b870ff0, 24bb875 (base f5f1cad). Feature now ON for user by default (smoothRegionBorders=true).

### Final whole-feature review (opus, range f5f1cad..24bb875) — Ready to merge: YES, no Critical/Important
Traced all 3 bake paths integrated: full/chunked-stepped/brush-partial all compose. Confirmed: RebakeAllStepped runs BakeFieldsRect whole-image before the chunked color pass → labels fully populated before any read, NO new seam (labels read only at own idx, never ±1 halo — unlike IsLand/CoastDistance); brush partial-rebake labels equal full-rebake at all touched pixels because the present-scan runs over the full padded rect and pad (1.5·mpd + glow + roundness·mpd) carries ≥1.5·mpd slack beyond the ≤roundness·mpd smoothing deviation; write-side gates (BakeFieldsRect: family FlatRegionFill&&SmoothRegionBorders; band +ElevationBands>1) are a superset of read-side gates (ColorForLandPixelFlat: family SmoothRegionBorders; band +ShowReliefLayer&&ElevationBands>1) → no read-without-write, no stale label at a colored pixel; (BiomeFamily)fLabel cast safe end-to-end (labels only ever land families or -1 because FamilyPriority omits Sea/Lake; even Biome.Ocean-painted-on-land → category Sea → not in priority list → never rasterized → stays -1 → FlatFamilyColor fallback → Coast, no crash); open-loop map-edge regions drop to -1 fallback (faceted, no crash); all-one/all-water safe. Compile integrity static-verified across files (using System in both .cs, overload resolution unambiguous at all TraceSmoothedLoops sites). All 5 new self-tests non-vacuous. 4 Minor:
  1. Latent cast fragility (MapRasterizer.cs:683): (BiomeFamily)fLabel→GetSlotColor has no Sea/Lake guard; safe TODAY only because FamilyPriority omits Sea/Lake — a future edit adding Sea/Lake or reordering the enum would make it a live ArgumentOutOfRangeException (the exact crash class that bit the flat-fill feature). Suggested defense-in-depth mirroring FlatFamilyColor: map Sea/Lake→Coast before GetSlotColor. NOTE: plan explicitly said "do not add extra guards" → plan-conflict, needs user decision.
  2. Verify serialized defaults in Editor (smoothRegionBorders=on, borderRoundness=1) — Unity deserialization gotcha (project hit 3×); user Editor eyeball, not statically checkable.
  3. Perf-only: family pass traces regardless of ShowBiomeLayer, band pass regardless of ShowReliefLayer, but ColorForLandPixelFlat ignores those labels when the layer is off — could gate the passes to skip wasted global traces.
  4. Accepted/no-action: ≤~2px self-healing halo sliver at the extreme outer recolor-rect edge on brush strokes (outside edited area, cosmetic, heals on next RebakeAll) — consistent with spec's accepted triple-junction approximation.
Not compiler/test-verified (Editor locked) — reviewer static-reasoned types/signatures/overloads.

### Post-review fixes (commit 9b08128..3184755) — Minors 1 & 3 applied at user's request ("обе")
- Minor 1 (crash-hardening): split FlatFamilyColor into two overloads — `(theme, Biome)` delegates to a new `(theme, BiomeFamily)` overload that does the Sea/Lake→Coast guard + GetSlotColor. ColorForLandPixelFlat's smoothed-label branch now calls `FlatFamilyColor(theme, (BiomeFamily)fLabel)` instead of casting straight into GetSlotColor → the label path now shares the guard, so a future FamilyPriority/enum edit letting a water family into a label can't reintroduce the "Sea/Lake нет плоского слота" crash (same crash class that bit flat-fill). Behavior-preserving for all existing callers (Biome overload unchanged; BakePaintedFields:436 fallback still passes Biome).
- Minor 3 (perf): gated the family category trace on ShowBiomeLayer and the band trace on ShowReliefLayer (previously ungated on layer flags) — ColorForLandPixelFlat ignores those labels when the layer is off, so the global traces were wasted.
- Re-reviewed (sonnet) Approved, ZERO findings: overload resolution unambiguous (Biome vs BiomeFamily distinct enums, no implicit conv), no recursion, and the write gates are an EXACT match to the read gates (family: FlatRegionFill&&SmoothRegionBorders&&ShowBiomeLayer; band: +ShowReliefLayer&&ElevationBands>1) — no read-without-write. Reviewer confirmed in code that SetShowBiomeLayer/SetShowReliefLayer call RebakeAll→CreateEmptyBuffers (resets labels -1) so no stale-label window on toggle; partial rebakes always run under current flags.
- Minors 2 (Editor serialized-defaults eyeball) & 4 (self-healing halo sliver, no action) remain for the user / accepted. Feature fully complete: commits 12f002d, b2e1dbb, 458e516, b870ff0, 24bb875, 3184755 (base f5f1cad). Branch stays as-is per standing decision (integration branch, unmerged, pending overall design direction).

## Live-testing: map editing hit-test was broken (pre-existing raster-branch bug, NOT curved-borders)
During the curved-borders Editor test the user found ALL map editing tools (brush/precise-selection/POI) silently did nothing. Root-caused via systematic-debugging (ruled out compile error and runtime exception via Editor.log; disabling smoothRegionBorders didn't help; user had never exercised editing on this branch). Added temporary [BRUSH DIAG] logging in BrushToolController.Update → `active=True cam=Main Camera pointerOverUI=False rayHitMap=False cells=2486`, then a second probe → `far(1e6,front)=False farBackface=True camPos=(250,750,250) rayDir=(0,-1,0)`. Root cause: `WorldMapRenderer.BuildQuadMesh` wound the map quad `{0,1,2,0,2,3}` → face normal -Y (down); the top-down camera's `meshCollider.Raycast` hit the quad's BACK face, which Unity ignores by default (Physics.queriesHitBackfaces=false) → `TryGetSiteHitPoint`/`GetCellUnderRay` always returned miss. Rendering was fine because the map material is double-sided (Sprites/Default, Cull Off). Fix (commit 5d6e6e2): reversed winding to `{0,2,1,0,3,2}` (normal +Y toward camera); UVs/vertices unchanged so hit.textureCoord (cellId) + hit.point stay correct. Diagnostic removed. User confirmed editing works.

## KNOWN FOLLOW-UP (deferred to next session by user): brush editing is VERY SLOW
User: "работает, но невероятно медленно, нужно подумать над путями ускорить — но это завтра." Leading hypothesis (not yet measured): each brush stamp → RebakeAffectedCells → RebakeRegion → BakeFieldsRect over a padded rect, which now (curved-borders, smoothRegionBorders on) does the coastline trace (global, O(corners)) PLUS one global TraceSmoothedLoops per present family PLUS one per present band, each followed by RasterizeRegionLabel that scans ALL loop edges per scanline row of the rect (O(rectH × total_edges), edges ×2^CoastlineSmoothness from Chaikin). On the 2486-cell map that's ~(1 + N_families + N_bands) global traces + full-loop-per-scanline rasterizations per stamp. Even the pre-curved-borders branch re-traced the coastline per stamp. Optimization directions to explore next session: (a) cache traced+decimated+smoothed loops from the last full bake and on a stamp re-trace only categories whose cells actually changed, reusing cached loops for the rest; (b) Y-bucket loop edges so RasterizeIsLand/RasterizeRegionLabel don't rescan all edges per scanline (this is the coastline review's deferred Minor #4); (c) clip tracing to the brush region; (d) profile first to confirm the dominant cost before optimizing. NOTE user also said upcoming work will CHANGE how the map is generated first, then continue visual improvements — so measure/re-plan against the new generation before investing in these optimizations.

## Brush-perf session (2026-07-08) — profiled, fixed A+B, then pivoted to GPU redesign
Followed systematic-debugging: instrumented per-stamp timing (Stopwatch breakdown of fields=lookup/trace/raster/coastDist + color + texture.Apply), had user run brush strokes in Play mode. **The recorded hypothesis above was WRONG about the dominant cost.** Measured (large brush, 2048 tex):
- `texture.Apply` ~0ms (the flagged suspect — eliminated).
- `trace` (the *hypothesized* culprit) only ~20-30ms — the SMALLEST field cost.
- Actual dominants: **`lookup` ~90-130ms** (per-pixel FindNearest over the rect) and **`raster` ~80-220ms** (all-edges-per-scanline).

Two fixes committed (df19c76), instrumentation removed:
- **Fix A** (`recomputeCellId` param): brush edits don't move Voronoi sites → the per-pixel cellId lookup is redundant on partial rebakes; skip it → `lookup`=0.
- **Fix B** (`CoastlineContour.EdgesOverlappingY`): prefilter loop edges to the rect Y-span before the scanline loop → `raster` ~180ms→~25ms (~7x).
Result: stamp ~280ms→~100ms (~2x) at large brush. Verified: 5 rasterizer self-tests pass + user confirmed map visually identical.

**User then required ≥25-30 fps** (still ~6fps after A+B on large brush). Root diagnosis: the whole map is a CPU single-thread software rasterizer (`MapRasterizer` → `Texture2D`; GPU only shows a `Sprites/Default` texture). Per-pixel color/coastDist over a huge rect can't hit 30fps on one core. User chose **GPU redesign**. Brainstorm decisions: visual EQUIVALENCE+improvements (not pixel-exact); **organic noise-warped borders** (domain warp in shader, not CPU Chaikin) → editing free at any brush size; **fragment-shader on the map material** (live, resolution-independent); texture-based data (no StructuredBuffer). Spec: `docs/superpowers/specs/2026-07-08-gpu-map-terrain-render-design.md` (a03847b). Plan: `docs/superpowers/plans/2026-07-08-gpu-map-terrain-render.md` (ebea26f), 13 tasks. Executing INLINE (executing-plans) — every task verified in the user's Unity Editor (subagents can't run Unity); shader-look tasks are interactive screenshot-tuning. Task 1 (this) = bank A+B, done. CPU path kept as fallback behind `useGpuRenderer` flag.

## GPU map render — IMPLEMENTED (2026-07-08, inline execution, all user-verified in Play mode)
Plan `docs/superpowers/plans/2026-07-08-gpu-map-terrain-render.md` executed. New `Assets/WorldGen/Rendering/GpuMap/` (`CellIdTexture`, `CellAttributeTexture`, `CoastDistanceTexture`, `GpuMapRenderer`) + `MapTerrain.shader`. `WorldMapRenderer` routes generation/load/brush/undo/overrides/layers through `GpuMapRenderer` when `useGpuRenderer` (default true); CPU path is the fallback.
- Tasks 1-8: cell-id + attribute textures + shader (flat family → domain-warp organic borders → elevation bands + hillshade + cold light → temperature tint + grain + vignette + water depth via coast-distance + wide coast glow). Visual EQUIVALENCE+ reached; tint reduced 0.38→0.1 (was washing land to fog). Region-border overlay defaulted OFF (`showRegionBordersLayer=false`).
- Task 9: brush/undo update the tiny attribute texture — **editing is now free/instant at ANY brush size** (was ~6fps). Biome/Relief layer toggles = instant shader uniforms (`_ShowBiome`/`_ShowRelief`). Display MODES (Height/Region/Biome) skipped — no UI exposes them.
- Task 10 (hit-test): `GetCellUnderRay` used CPU `rasterBuffers` (null in GPU mode → precise selection returned nothing) — fixed to raycast→`NearestCellLookup`.
- Task 11: coast-distance recomputed once on `EndBrushStroke` if topology (land/water) changed; optimized ~16× (cached cellId array + 1/4-res chamfer, bilinear).
- Task 12: 7 override methods → cheap `RefreshAfterCellDataChange`; generation chunked via `BuildAllStepped` (live progress bar). Fallback flag `useGpuRenderer`.
- Task 13: final review (subagent, static — Unity not runnable by agents) + attr-texture leak fixed.
**Extra features user requested mid-flow:** (a) **water brush** — `BrushTool.Water` with Океан/Суша modes; Суша gives new land a per-cell fbm elevation so it's RELIEF-varied, not a flat plateau. (b) **brush-based area SELECTION** (replaced per-cell): paint a radius area, Shift accumulates multiple areas, own `selectionRadius` slider (independent of terrain brush) + yellow cursor ring preview.
Known deferred: display-mode shader branches (no UI); coast-glow at 1/4-res is slightly softer (acceptable). Scene committed (`SampleScene.unity`: `showRegionBordersLayer:0`, `useGpuRenderer:1`). Final code review (subagent, static) clean except one Mesh leak (`CellSelectionController.RebuildOverlay` allocated a Mesh/frame during selection drag without destroying the old one) — FIXED (commit 0741e66) + `OnDestroy` cleanup added. Old `.dndproj` saves load fine (cell-data model unchanged; GPU renders the same fields) — user-verified.

## NEXT (planned, restart fresh — user's choice 2026-07-08): remove the CPU render path (GPU-only)
User decided: **remove the CPU software-rasterizer entirely** — reasoning: it loads one CPU core HARDER than the GPU shader, so it's the WORSE path for weak hardware (the whole point of the GPU move was to offload weak machines). Not insurance. Plan written: `docs/superpowers/plans/2026-07-08-remove-cpu-render.md`. Deletes `MapRasterizer.cs` + `CoastlineContour.cs`, guts the CPU raster members/self-tests/`useGpuRenderer` flag from `WorldMapRenderer` (GPU always). KEEP: `NearestCellLookup`/`MapPalette`/`Noise` (GPU/brush use them), `RegionColorPalette` (legend/UI), `PolygonTriangulator` (selection overlay). 2-stage: (1) flag-removal→GPU-only, verify; (2) delete dead code, verify. Then `main` can merge clean GPU-only. **Separate future topic (NOT that plan):** reimplement coastline smoothing / curved biome borders / graphical polish (CPU vs GPU TBD) — GPU shader already has its own organic warp borders + coast glow, so nothing regresses now. Branch kept as-is (un-merged) meanwhile; worktree `.claude/worktrees/map-terrain-raster` preserved.


## Sub-project A execution (2026-07-08): worldgen shape/relief/beach
Spec: docs/superpowers/specs/2026-07-08-worldgen-shape-relief-beach-design.md (7910aba)
Plan: docs/superpowers/plans/2026-07-08-worldgen-shape-relief-beach.md (c8fb387)
Base: c8fb387. Executing via subagent-driven-development (sonnet impl+review). NOTE: agents can't run Unity → compile + [ContextMenu] self-tests are the USER's Editor step; reviews are static hand-traces.

Task 1 (A1 radial island falloff): complete (commit c8fb387..7497017, review clean — Spec OK / Quality Approved on sonnet; reviewer hand-traced falloff math, both HeightmapGenerator call sites, dual BuildGenerationParams, scene YAML edit, self-test determinism). 2 Minor (deferred to final triage): (a) stale "Стандартное значение 0.5" doc in GenerationParams.InnerRadius XML + WorldMapRenderer.innerRadius tooltip; (b) WorldMapRenderer C# field defaults falloffPower=2.5f/innerRadius=0.5f left unchanged (only scene serialized values bumped) -> a NEW WorldMapRenderer instance gets old shape until Inspector-corrected. Sync all 3 default sources in final cleanup.

Task 2 (A2 elevation contrast): complete (commit 7497017..d27b5cc, review clean — Spec OK / Quality Approved on sonnet, ZERO findings; reviewer hand-traced ApplyContrast formula + ordering before Classify/cell.Height, both call sites pass ElevationContrast AND keep BeachElevationThreshold, both BuildGenerationParams wired, self-test non-vacuous (contrast=1 identity spread 0.40 -> contrast=1.5 spread 0.60, midpoint invariant). Impl added a [Tooltip] for sibling-field consistency — benign.)

Task 3 (A3 coastal beach): complete (commit d27b5cc..9ed16bc, review clean — Spec OK / Quality Approved on sonnet; reviewer grep-verified ZERO BeachElevationThreshold residue in Assets/, BiomeClassifier.cs diff empty (untouched), ApplyToCells new arity + BeachClassifier called after in BOTH paths, ordering safe (beach before RegenerateTemperature which only reads Height), WorldMapRenderer.beachElevationThreshold serialized field + 11 CellOverrideService sites intact, BeachClassifier skips IsOcean||Lake & marks land Beach iff neighbor IsOcean via TryGetValue, pure C#, self-test fixture non-vacuous, VoronoiCell(int,System.Numerics.Vector2) ctor matches). NOTE: BeachClassifier.cs.meta NOT committed (Unity generates on import) — USER must commit the .meta after Editor import. 1 Minor (deferred): self-test doesn't directly exercise land-adjacent-to-lake-only exclusion (logic verified correct by hand via isOcean/isLake mutual exclusivity); optional 6th fixture cell would close it.

Task 4 (companion camera bg = ocean): complete (commit 9ed16bc..d8cf503, review clean — Spec OK / Quality Approved on sonnet; reviewer verified theme source is the REAL field paletteTheme (WorldMapRenderer.cs:113, same value fed to gpuRenderer.BuildAll/BuildAllStepped), MapPalette.GetSlotColor(theme,Abyss)->Color32->backgroundColor implicit conv valid, clearFlags=SolidColor + targetCamera null-guard, 9 insertions/0 deletions single file. Independently DISPROVED the "bg set once per session goes stale" concern: grep found NO runtime paletteTheme mutation path anywhere -> inert today (forward-looking caveat only if a live theme switcher is added). 2 Minor: redundant fully-qualified names + redundant null-guard, both verbatim from brief, cosmetic.)

## Accumulated Minors for FINAL-REVIEW triage (sub-project A)
- T1a: stale "Стандартное значение 0.5" doc — GenerationParams.InnerRadius XML + WorldMapRenderer.innerRadius tooltip (now 0.2).
- T1b: WorldMapRenderer C# field defaults falloffPower=2.5f/innerRadius=0.5f NOT synced with scene (only scene serialized values bumped) -> a NEW WorldMapRenderer instance gets old shape until Inspector-fixed. Sync all 3 default sources.
- T3: self-test doesn't exercise land-adjacent-to-lake-only beach exclusion (logic verified correct by hand).
- T4: redundant fully-qualified MapPalette/PaletteSlot names + redundant targetCamera null-guard (cosmetic, from brief).
- FOLLOW-UP (user, Editor): commit generated BeachClassifier.cs.meta after Editor import; verify new Inspector defaults (coastRoughness 0.2 / continentCenterJitter 0.18 / elevationContrast 1.5) per Unity serialization gotcha; run the 3 new self-tests; regenerate + eyeball shape/relief/beach + ocean-bg blend.

### Final whole-branch review (opus, range c8fb387..d8cf503) — Ready to merge: WITH FIXES (fix applied)
Cross-task composition verified: both generation paths (GenerateWorld + GenerateWorldStepped) equivalent (identical 11-arg HeightmapGenerator ctor, ApplyToCells + BeachClassifier in same position); A1xA2xA3 compose (contrast applied before Classify but beach is coast-adjacency, so contrast can't change WHICH cells beach; elevation-beach fully suppressed via 0f + ApplyContrast clamp >=0); beach model isolated from live-edit (BiomeClassifier.Classify untouched; WorldMapRenderer.beachElevationThreshold + 11 CellOverrideService sites intact; zero GenerationParams.BeachElevationThreshold residue in .cs); ordering safe (beach skips ocean/lake; LakeRegionUnifier keys off Biome==Lake, untouched; RegionGrowing reads IsOcean; temp reads Height); backward-compat SOLID (saves persist cells+GenerationParams, load uses stored cells no-regen, Newtonsoft default Ignore drops removed field, new fields fall back to C# defaults). 1 Important T1b GATED + FIXED (commit 18c70fc): WorldMapRenderer C# initializers falloffPower/innerRadius were stale 2.5/0.5 vs new 1.8/0.2 -> synced + T1a stale docs. Deferred Minor: T3 (self-test lacks land-adjacent-to-lake-only case, logic hand-verified), T4 (redundant qualifiers + DEAD targetCamera null-guard — deref'd unconditionally 11 lines earlier — cosmetic, verbatim from brief).
Sub-project A COMPLETE (code + static review): commits 7497017, d27b5cc, 9ed16bc, d8cf503, 18c70fc (base c8fb387).
NOT compiler/Editor-verified (agents can't run Unity). USER Editor pass PENDING: (1) commit generated BeachClassifier.cs.meta after import; (2) verify Inspector defaults coastRoughness 0.2 / continentCenterJitter 0.18 / elevationContrast 1.5 (Unity serialization gotcha); (3) run 3 new [ContextMenu] self-tests (Island Shape Ocean Border, Elevation Contrast Widens Range, Coastal Beach Classification) -> all PASS; (4) regenerate + eyeball irregular continent / stronger relief / 1-cell beach rim / ocean-bg edge blend. Branch stays un-merged (standing decision) pending B (vector render).

## Follow-up: ocean padding (enlarge-canvas) + jitter default 0.01 (2026-07-08)
User feedback after A Editor test: continent off-center (continentCenterJitter 0.18 too strong) + wants more ocean around it while PRESERVING continent size/detail. Chose enlarge-canvas (Option 2) over shrink-continent (Option 1) despite ~2x cell/gen-time cost (informed).
Brief: docs/superpowers/plans/2026-07-08-worldgen-ocean-padding.md (a14b281).
Design (two-field, no runaway): user sets continentWidth/Height + oceanPadding; mapWidth/Height are DERIVED = continent*(1+2*oceanPadding), recomputed each gen in BuildGenerationParams from STABLE continentWidth (never mapWidth) -> no runaway; render/camera/GPU UNTOUCHED (already frame mapWidth/Height). HeightmapGenerator falloff moved to core-relative coords (originX/Y + coreWidth/Height) -> continent keeps absolute size, padding ring auto-ocean (points outside core give border>1 -> falloff=1). Poisson/Voronoi run on full domain (p.Width). Size presets set continentWidth. continentCenterJitter default 0.18->0.01.
Impl commit 7a13088 (DONE_WITH_CONCERNS: scene never serialized continentCenterJitter -> C# default 0.01 applies, no scene edit needed — benign, verified). Constructor sig changed (mapWidth/Height -> coreWidth/Height/originX/Y); all callers migrated (2 prod WorldGenerator + 4 self-test incl. updated SelfTestIslandShapeOceanBorder to origin0/core500). New SelfTestOceanPaddingFramesContinent.
Review (opus, a14b281..7a13088): READY TO MERGE, NO code fixes — 8/8 named risks pass (callers migrated + no stale mapWidth ref in HeightmapGenerator, falloff core-relative hand-traced, runaway-safe incl round-trip, both gen paths byte-identical, render untouched, presets->continentWidth, self-tests non-vacuous, scene serialization verified absent). 2 Minor (no action): (a) ProjectSerializerSelfTests.cs:21 GenerationParams lacks 3 new fields -> default 750/750/0.2, benign (round-trip only, never generates; latent trap ONLY if Width<ContinentWidth -> all-ocean, not a prod path); (b) scene mapWidth:750/mapHeight:750 now stale/derived (overwritten to 1050 on first gen, harmless; optional [HideInInspector] since user now sets continentWidth instead).
USER Editor pass PENDING: compile clean; Inspector eyeball continentCenterJitter=0.01 + continentWidth/Height=750 + oceanPadding=0.2 (save scene to persist); run self-tests (Ocean Padding Frames Continent + the 3 from A); regenerate -> continent same size + visible ocean ring + camera frames full ~1050 domain; tune oceanPadding slider. STILL PENDING from A: commit generated BeachClassifier.cs.meta after import.

## Sub-project B execution (2026-07-08): vector region render
Spec: docs/superpowers/specs/2026-07-08-vector-region-render-design.md (0a1692b)
Plan: docs/superpowers/plans/2026-07-08-vector-region-render.md (48dd3d5), 6 tasks
Base: 48dd3d5. Subagent-driven (sonnet impl+review; opus final). NOTE: agents can't run Unity; Tasks 4-5 are SHADER (HLSL written statically, user visual-verifies+tunes in Editor); C# tasks self-tested by user.

Task 1 (RegionCategories extract): complete (commit 48dd3d5..e58ef92, review clean — Spec OK / Quality Approved on sonnet; byte-identical extraction verified, both MapRasterizer call sites re-qualified, grep zero orphaned refs in Assets/, FlatFamilyColor untouched, public static/same-namespace, self-test non-vacuous). 2 Minor (plan-mandated, ledger for final triage): (a) WorldMapRenderer self-test dead local `var RC = typeof(...)` -> CS0219 unused-var warning, remove for pristine output; (b) FamilyPriority public mutable array (harden to IReadOnlyList, defer). USER: commit generated RegionCategories.cs.meta after import.

Task 2 (RegionLabelBaker + RegionLabelTexture): complete (commit e58ef92..901cc5c, review clean — Spec OK / Quality Approved on sonnet; signatures match CoastlineContour+RegionCategories exactly, BakeCategory is faithful rect-scoped port of RasterizeSmoothedCategoryRect with correct priority-overlap (later wins), RG16 encode + 255-sentinel for -1 correct no channel-swap, self-test non-vacuous via REAL pipeline CornerGraphBuilder->NearestCellLookup->CellIdTexture->BakeRect). 1 Important (plan-mandated) FIXED (commit 4db0869): RegionLabelTexture.PatchRect Apply() did FULL-texture SetPixels32 -> changed to rect-scoped SetPixels32(rectX,rectY,rectW,rectH,rectPixels)+Apply(false), CPU pixels cache kept coherent; avoids per-stroke full-texture reupload = serves spec perf requirement #1 (brush stays fast). 1 Minor: .meta of RegionLabelBaker.cs/RegionLabelTexture.cs pending USER commit after import.

Task 3 (bake label texture in GpuMapRenderer + thread corners): complete (commit 4db0869..d5628e6, review clean — Spec OK / Quality Approved on sonnet, ZERO findings; both BuildAll/BuildAllStepped callers (WorldMapRenderer RebakeAll:2462 + RebakeAllStepped:2620 GPU branches) pass corners, no stale-arity; corners provably non-null (CornerGraphBuilder.Build set before RebakeAll at gen 246/load 278); FinishBuild bakes labels AFTER cellIdArray fill + attr, before coast; BakeRect full-image (0,0,texW,texH) + bakedSmoothing=2/decimation=0/bands=5, bands matches shader _ElevBands=5; NO texture leak on regen (RegionLabelTexture.Build destroys-before-reassign + labelTex instance reused + OnDestroy frees) — the leak class that bit this branch before, handled; no existing-render regression, pure insertion). Shader doesn't read _LabelTex yet (Task 4).

Task 4 (shader: fill from labels, remove domain-warp): complete (commit d5628e6..d40c989, STATIC review clean — Spec OK(static) / Quality Approved on sonnet; grep zero warpUV/_Warp*/_Seed remaining, labelAt int2 declared-before-use, famL fallback + _Palette[famL] provably 0-10 in-bounds, band fallback, wuv=i.uv clean substitution all downstream intact, fbm/vnoise/hash21 kept, hillshade/tint/water/glow/vignette/outline byte-identical mod wuv, 3 warp SetFloat removed from GpuMapRenderer). 2 Minor (no action): (a) band-normalization coupling bakedBands=5 <-> _ElevBands=5f must stay lockstep (latent, plan risk already notes; add clamp/comment when Task5 touches); (b) RG16 name is 8-bit-per-channel (correct, misread-prone). VISUAL correctness + smoothing tuning = USER Editor step. FIRST visible change: fuzzy warp borders -> crisp smoothed label borders.

### BUG found during Task 4 user visual-verify (systematic-debugging) + FIXED
Symptom: land uniform (family-0 Coast color) despite Biome layer ON — "nothing changed". Root cause: RegionLabelTexture used TextureFormat.RG16 + SetPixels32; Unity's SetPixels32/SetPixels work ONLY on RGBA32/ARGB32/RGB24/Alpha8 — for RG16 they are SILENTLY IGNORED -> label texture never populated -> shader reads 0 -> famL=0 everywhere -> whole land = _Palette[0] (Sea->Coast) tan. (Task 2 self-test missed it: tested CPU label BUFFERS, not the GPU texture round-trip.) FIX (commit ecc5f08): RG16->RGBA32 (SetPixels32-supported; R=family G=band B/A unused; shader .rg unchanged) + new SelfTestRegionLabelTextureRoundTrip (Build->GetPixels32->assert encoded bytes incl 255 sentinel; would fail on RG16 (GetPixels32 also RGBA32-only), pass on RGBA32). Memory: 2048^2 RGBA32 = 16MB (was 8MB) - acceptable. AWAITING user re-verify in Editor (compile, run round-trip self-test, regenerate -> expect varied biome colors with crisp smoothed borders).

### BUG #2 found during Task 4 re-verify (systematic-debugging) + FIXED
After RGBA32 fix, land STILL uniform grey-tan (Coast). Discriminated via shader debug (famL=family -> varied biomes = generation OK) + bake histogram (label family proportions MATCH cell proportions almost exactly: Forest+ForestWarm ~58% of land, label(-1)=68.7% = mostly water/padding -> BAKE IS CORRECT). Root cause: RGBA32 texture defaults to sRGB (color); shader tex2Dlod applies sRGB->linear gamma on sample, CRUSHING integer label bytes (Forest=7 -> 7/255 sRGB-decoded -> ~1/255 -> famL~0-1 -> Sea/Lake->Coast) -> whole land collapses to Coast tan-grey. Round-trip self-test PASSED and missed it because GetPixels32 reads RAW bytes (bypasses sRGB) - only shader sampling is corrupted. FIX (commit aa8edfd): create label texture LINEAR (new Texture2D(w,h,RGBA32,false, true)) - it's DATA not color. No unit-test guard possible (shader-sampling issue). AWAITING user re-verify: expect varied biome colors (green-dominant forest, snow center, tan edges) with crisp smoothed borders.

### Task 4.5: live border roundness/smoothing (user request during Task 4 verify)
User wants rounder borders (recalled the old CPU curved-borders "skip vertices" = decimation). Machinery already there (CoastlineContour.DecimateClosedLoop), was OFF (bakedDecimation=0). Implementer found WorldMapRenderer ALREADY has borderRoundness (float, def 1) + coastlineSmoothness (int, def 3) fields from the old CPU curved-borders feature. Chose (a): reuse them (no new fields, no CS0102, CPU path slated for removal). commit 5882c89: GpuMapRenderer.SetContourParams(smoothing, decimation) setter; both RebakeAll(:2488)+RebakeAllStepped(:2647) GPU branches call gpuRenderer.SetContourParams(coastlineSmoothness, borderRoundness * minPointDistance) before Build. Bake-time params (not live uniform) -> regenerate to see. USER tunes borderRoundness (bump 1->2-3 rounder) + coastlineSmoothness in Inspector + regenerate.

### Task 4.6: smoothed land/water coastline (user clarified: they meant land/water edge, not inter-biome)
Coastline was still faceted (shader decided land/water per-cell attr.a) while biome borders were smoothed. Fix (commit c6e3a47): RegionLabelBaker bakes a smoothed isLand mask (CoastlineContour water predicate + RasterizeIsLand) into label-texture B channel (255=land,0=water); RegionLabelTexture Build/PatchRect/Encode gain the mask; GpuMapRenderer has isLandMask field, wired. Shader: new landAt(uv) reads B (LINEAR tex, exact); renderWater=!landAt; water type wt=ocol if smoothed-water over land cell defaults ocean; coastal dark-outline 4-tap now uses landAt (follows smoothed coast). Coast-distance/glow left per-cell (soft/wide, acceptable). Same borderRoundness/coastlineSmoothness sliders control coast roundness (bakedSmoothing/Decimation reused for the water trace). All BakeRect + RegionLabelTexture.Build/PatchRect callers + 2 self-tests updated. Minor: smoothed-water-over-lake-cell -> ocean color (thin sliver); waterAt now dead (harmless). USER re-verify: coastline rounder + tunable via borderRoundness.

## Task 5 + tuning + Task 6 (2026-07-08/09)
Task 5a biome line (ca038bd): thin dark line between biome families, Coast(2)+255 excluded (beach zone gets no line). Task 5b soft beach (4ef288b) via land->water distance field (CoastDistanceTexture inverted predicate, 0 on water growing inland). Tuning rounds: beach saturation pow+strength (ccbe4eb), CoastRoughness randomized per-seed [0,0.5] (ccbe4eb, serialized field removed), live beach sliders width/strength/hardness (d89068b, OnValidate live in play mode), beach color picker + disabled-by-default strength=0 (e6ef8ce). border roundness/coastlineSmoothness sliders wired into GPU bake (5882c89, reuse old CPU curved-borders fields). smoothed land/water coastline (c6e3a47): isLand mask in label-texture B channel, shader landAt() decides land/water + outline follows smoothed coast (user wanted rounded coastline not inter-biome).

Task 6 brush edit integration (commit e0cf909, review Approved on sonnet, all 8 named-risk checks pass; + fix 14cd68d): faceted label patch (family/band/isLand) per touched cell in UpdateCells (only where cellIdArray==cell.Id) + accumulate dirty rect + PatchRect once/stamp; FinalizeLabels() smoothed re-bake of padded dirty rect on stroke-end; EndBrushStroke + RefreshAfterCellDataChange call FinalizeLabels; RebakeAffectedCells (per-stamp) faceted-only. bakedCellById holds live cell refs (VoronoiCell is class, brush mutates in place). Self-test SelfTestFacetedLabelPatch (flip cell to ForceOcean, assert B channel 255->0 instant on same texture). FIX 14cd68d: FinalizeLabels pad derived from bakedDecimation (was fixed 48px -> stale-edge risk at high borderRoundness) + stale edit-path docs. Minors (final-triage): per-stamp uploads whole accumulated rect (grows over long stroke, per-design); RefreshAfterCellDataChange now full-map label rebake (undo/bulk-override cost). USER re-verify: recompile+regenerate, test water/biome/elevation brushes (instant faceted -> smoothed on release), run SelfTestFacetedLabelPatch.

### Sub-project B FINAL whole-branch review (opus, 48dd3d5..14cd68d) — READY TO MERGE: YES
No Critical/Important. Verified: signature threading consistent all 16 commits (BuildAll/BuildAllStepped+corners, BakeRect/PatchRect/Encode+isLandMask, SetBeachParams+color, SetContourParams); both texture bugs fixed+guarded (RGBA32 + linear:true); texture-leak audit CLEAN all 5 textures (destroy-before-reassign + OnDestroy) — the leak class that bit branch twice, closed; RegionCategories extraction clean no orphan; label pipeline consistent end-to-end (R/G/B channels, 255 sentinels, LINEAR read, isLand drives land/water + outline); edit integration composes (live cellById refs, finalize wiring); merge-ready (.meta committed bceaea1, scene re-serialized stale coastRoughness gone, no dangling refs). 3 Minor all DEFER: unguarded cellById[-1] (FIXED anyway, commit adds TryGetValue guard), dead waterAt shader fn (tidy in CPU-removal), band-normalization coupling (both hardcoded 5). Beach: shipping scene has beachStrength=0.365 (user re-enabled). Recommend merge from HEAD (bceaea1+). Old .dndproj load unaffected (cell-data model unchanged).

## Sub-project C execution (2026-07-09): map decorations — iso terrain sprites
Spec: docs/superpowers/specs/2026-07-09-map-decorations-iso-sprites-design.md (2528b68)
Plan: docs/superpowers/plans/2026-07-09-map-decorations-iso-sprites.md (8f296f8), 8 tasks
Branch: feat/map-decorations-iso-sprites (from main@9d2c7c4). Base for Task 1: 8f296f8.
Subagent-driven (sonnet impl+review; opus final). NOTE: agents can't run Unity → implementers
write code + STATIC hand-trace self-review + commit (report DONE_WITH_CONCERNS); [ContextMenu]
self-tests + visual/build verification are the USER's Editor step, batched at checkpoints
(after Tasks 1-4 C#, after Tasks 5-8 render/integration). Reviews are static.
Task 1: complete (commit 8f296f8..931c57e, review clean — Spec ✅ / Quality Approved on sonnet; both files byte-identical to brief, no collision, TypeDensity prose-drift correctly omitted). USER: commit generated .meta after import.
Task 2: complete (commit 931c57e..b8b19a5, review clean — Spec ✅ / Quality Approved on sonnet; TryClassify/ReliefStyle/IsColdCell match brief byte-for-byte, priority Snowy>Forested>Bare correct, style deterministic-not-hash; implementer caught+fixed a REAL CS0104 System.Numerics.Vector2 vs UnityEngine.Vector2 ambiguity (fully-qualified), Effective* verified to resolve to Height/Temperature/IsOcean. 3 Minor DEFER (coverage gaps in self-test, inherited from brief): Hill never asserted, Snow/Tundra-family cold branch never exercised, no boundary-value asserts. USER: run Self-Test: Decoration Classify in Editor + commit .meta.)
Task 3: complete (commit b8b19a5..08082ef, review clean — Spec ✅ / Quality Approved on sonnet; Place/PlaceRect/PlaceType/TintFor match brief verbatim, cap logs warning + sorts, rect==full invariant hand-traced correct, determinism/non-water/no-CS0104 confirmed. Implementer caught+fixed a REAL brief bug: Fixture never set VoronoiCell.Polygon → NearestCellLookup drops Polygon.Count<3 → fixture empty → self-test always FAIL; fixed with 4-corner square polygons (verified against NearestCellLookup.cs:32). 2 Minor DEFER: rect==full asserts count-equality not per-instance (brief-inherited, sound proxy); TintFor 'or'-pattern new to codebase but valid C#9. USER: run Self-Test: Decoration Placement + commit .meta.)
Task 4: complete (commit 08082ef..ec13b74, review clean — Spec ✅ / Quality Approved on sonnet; atlas RGBA32+SetPixels32 (NOT RG16), no linear:true, Y-flip provably correct vs Unity bottom-to-top, packing bounds generic-safe, UvRect fallback NRE-guarded via || short-circuit, all verbatim to brief. 3 Minor DEFER: (1) DrawHill ignores style → 3 identical Hill atlas slots (cosmetic placeholder, real-art fixes); (2) zero tile-padding + Bilinear → possible dark-edge bleed at UV-rect borders — CARRY TO Task 6 (half-texel UV inset or padding); (3) catalog self-test samples 1-2 of 10 slots. USER checkpoint A: run 3 self-tests (Classify/Placement/Catalog) + dump atlas PNG to eyeball mountain peak-up + commit .meta.)
Task 5: complete (commit ec13b74..097e6ba, review clean — Spec ✅ / Quality Approved on sonnet; shader 'WorldGen/Decorations' byte-for-byte standard Unity instanced-unlit boilerplate, all instancing macros correct, UV/tint/clip/transparent state exact. User's live Editor auto-imported clean (0 shader errors in Editor.log). shader .meta guid = 31f2c34c97ad8014caf4c644bbc46b49 (for Task 8 Always Included Shaders). 1 Minor FYI: v2f.tint uses COLOR semantic (fine on Unity6 default backends). USER: commit shader .meta.)
Task 6: complete (commit 097e6ba..2676197, review clean — Spec ✅ / Quality Approved on sonnet; exact brief transcription + 2 verified fixes: (a) material.mainTexture→SetTexture('_Atlas',...) at BOTH Init+LateUpdate sites (mainTexture binds only _MainTex → would've been blank sprites, no compile error; confirmed vs GpuMapRenderer convention), (b) controller-directed half-texel UV inset (bilinear-bleed guard, tx/ty hoisted). Batch loop/MPB/SetVectorArray(List<Vector4>)/enableInstancing/OnDestroy all correct. 3 Minor: Visible is field not prop (brief); EnsureResources in LateUpdate dead (brief); **FIX IN TASK 8: EnsureResources' 'if(material.shader==null)' guard is UNREACHABLE — new Material(Shader.Find(null)) throws first (the v0.2.0 crash class); make it check Shader.Find result BEFORE constructing Material.** USER: Editor didn't auto-reimport this file — focus Unity + Console-check before/at checkpoint B.
Task 7: complete (commit 2676197..f93f46a, review clean — Spec ✅ / Quality Approved on sonnet; +49/-0 additive-only into WorldMapRenderer.cs, verified line-by-line no existing logic touched. Hooks: RebuildDecorations at GenerateAndRender:278 (before OnWorldRegenerated) + FinishLoadFromCells:320 (both invoke sites covered, no double-add), RefreshDecorationsRect(fullMapRect) at EndBrushStroke:556, RebuildDecorations at RefreshAfterCellDataChange:2661, guarded line into pre-existing single OnValidate (no CS0111). Hook reachability traced end-to-end (cells/nearestLookup populated before each). API sigs match Tasks 1-6. 2 Minor DEFER: full-map rect on every stroke-release (brief-sanctioned perf cost, no dirty-rect tracked); verbose fully-qualified List (brief). USER: Play-mode verify + eyeball Inspector decorationConfig defaults (Unity serialization gotcha).
Task 8: complete (commit f93f46a..0a352fd, review clean — Spec ✅ / Quality Approved on sonnet; SetShowDecorations mirrors SetShowBiomeLayer, MapLayersPanel row uses REAL AddLayerToggleRow (brief's AddLayerRow didn't exist) matching sibling rows, GraphicsSettings.asset line for Decorations shader (guid 31f2c34c…) well-formed in m_AlwaysIncludedShaders + MapTerrain intact, DecorationRenderer null-guard now EFFECTIVE (Shader.Find checked before new Material, early return). Implementer correctly dropped controller's erroneous meshRenderer line (DecorationRenderer draws via DrawMeshInstanced, no MeshRenderer). 2 Minor no-action. USER checkpoint B: verify Graphics→Always Included Shaders shows Decorations resolved (re-add via UI if Editor reverted), toggle works, LOCAL BUILD renders decorations.)

### Sub-project C FINAL whole-branch review (opus, 9d2c7c4..0a352fd) — READY TO MERGE: YES with fixes
Cross-task composition CONFIRMED end-to-end: placer→catalog→renderer→shader contracts match (UvRect offset/size ↔ shader r.xy+uv*r.zw, Color32→tint, _Atlas via SetTexture at both sites), instancing mechanism canonical (multi_compile_instancing+enableInstancing+MPB SetVectorArray sized n), integration hooks fire exactly once/gen with cells/nearestLookup populated, both v0.2.0 shader-strip defenses in place (Always Included guid byte-match + Shader.Find null-check before new Material), palette-slot safe (no BiomeFamily overload → FamilyToSlot throw unreachable; water/lake never decorated), zero CS0104. Implementer improved on plan drafts in 3 spots that would've shipped bugs (mainTexture→SetTexture, guard order, CS0104 in self-test).
1 IMPORTANT (fix before merge): decorationCatalog.Atlas (256×256 RGBA32) never Destroy()-ed on WorldMapRenderer teardown — one-time ~256KB leak, but on-pattern for project leak history. Fix: free in OnDestroy.
VISUAL-VERIFY (acted on): sortZ ascending gives INVERTED overlap (camera Euler(90,0,0): cam-up→world+Z so +Z=screen-up; south/lower-Z sprites should draw last/on-top → needs DESCENDING). Controller confirmed via rotation math; flipping to descending in the fix.
Minor DEFER: DrawHill ignores style (tint still differentiates; art swappable); atlas 256² placeholder res; EndBrushStroke full-map rect + PlaceRect skips maxInstances cap (perf-only); self-test coverage gaps; Visible field-not-prop.
USER checkpoint B (static review can't cover): per-instance UV/tint actually varies in play; POI on top (queue 2990<3000); layer toggle + brush stroke-end + live sliders (watch OnValidate warning); LOCAL BUILD renders decorations (v0.2.0 strip class); perf on dense map.
Final-review fixes applied (commit 0a352fd..d1aa547): (1) IMPORTANT atlas-leak — WorldMapRenderer.OnDestroy now frees decorationCatalog.Atlas; (2) depth-sort flipped ascending→DESCENDING at both sites (DecorationPlacer.Place + RefreshDecorationsRect) so south/nearer sprites draw on top. Diff surgical +3/-2. Branch feat/map-decorations-iso-sprites now: all 8 tasks + final review + fixes complete, reviewed clean. AWAITING USER checkpoint B (Play + local build) before merge to main.

### Checkpoint B build bug RESOLVED — instancing variant stripping
User's local build showed NO decorations (Editor fine). Player.log clean — NO "shader not found" (Task 8 guard silent → shader WAS in build via Always Included Shaders; NOT the v0.2.0 class). Root cause: m_InstancingStripping=0 (StripUnused) in GraphicsSettings + NO serialized .mat uses WorldGen/Decorations → Unity strips the INSTANCING_ON variant of the runtime-only DrawMeshInstanced shader → in build the instanced draw renders nothing (Editor has all variants). Fix: Graphics → Shader Stripping → Instancing Variants → "Keep All" (m_InstancingStripping 0→2). User applied + rebuilt → decorations render in build. CHECKPOINT B FULLY PASSED (Editor pines-fixed + build renders). Note: enum confirmed 2=KeepAll. Alt fix (not taken): serialized instanced material in Resources.
Finalizing branch: committing 9 generated .meta + SampleScene.unity (decorationConfig serialized) + GraphicsSettings (KeepAll) + ledger. Excluded as unrelated Editor churn: ProjectSettings.asset (InputSystem preloadedAssets), UnityConnectSettings (m_Enabled 0→1), settings.local.json. Merging to main next (user chose merge-then-POI).
